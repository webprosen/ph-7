const numbers = [12, 34, 22, 43, 65];

const student = {
    name: 'prosen',
    age: 25,
    class: 10
};

const string = `My name is ${student.name}`;

const myFn = () => 10;

const isEven = x => x%2 == 0;

const newNumbers = [...numbers, 200];

console.log(newNumbers);